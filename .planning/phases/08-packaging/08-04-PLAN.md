---
phase: 08-packaging
plan: 04
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - tools/smoke_phase8.py
  - .planning/phases/08-packaging/08-HUMAN-UAT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [PKG-05, PKG-06, PKG-07]
user_setup:
  - "Linux package build/UAT requires Ubuntu 24.04 or compatible Debian build tooling; on macOS, record build/install gates as pending manual UAT."
must_haves:
  truths:
    - "Full Phase 1-8 source/static smoke passes before Phase 8 closeout."
    - "If Debian tooling is present, dpkg-buildpackage and lintian are run and results recorded."
    - "If Debian tooling is absent, the executor records Linux package UAT as pending without pretending it passed."
    - "Phase 8 closeout clearly distinguishes automated local checks from Ubuntu VM/hardware checks."
  artifacts:
    - path: ".planning/phases/08-packaging/08-04-SUMMARY.md"
      provides: "Final packaging execution summary and build/UAT status"
    - path: ".planning/phases/08-packaging/08-HUMAN-UAT.md"
      provides: "Updated signoff checklist if Linux build/install was deferred"
---

<objective>
Close Phase 8 with full source/static regression, optional Linux package build/lintian checks when tooling is available, no-pyc package verification, and explicit VM install UAT status.
</objective>

<execution_context>
@/Users/sushilkumarsahani/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sushilkumarsahani/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-packaging/08-RESEARCH.md
@.planning/phases/08-packaging/08-PATTERNS.md
@.planning/phases/08-packaging/08-VALIDATION.md
@.planning/phases/08-packaging/08-HUMAN-UAT.md
@tools/smoke_phase1.py
@tools/smoke_phase2.py
@tools/smoke_phase3.py
@tools/smoke_phase4.py
@tools/smoke_phase5.py
@tools/smoke_phase6.py
@tools/smoke_phase7.py
@tools/smoke_phase8.py

<interfaces>
- Source/static smoke must be the authoritative local gate on non-Linux hosts.
- `dpkg-buildpackage`, `lintian`, and `apt install ./acercontrol_*.deb` are Linux-only gates.
- Phase 8 summaries must preserve exact commands run and distinguish skipped tooling from passed tooling.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add final package verification helpers</name>
  <files>tools/smoke_phase8.py</files>
  <read_first>
    @tools/smoke_phase8.py
    @.planning/phases/08-packaging/08-VALIDATION.md
  </read_first>
  <action>Extend `tools/smoke_phase8.py` full mode with final local checks: no `.pyc` paths referenced by `debian/acercontrol.install` or `install.sh`; `debian/rules` executable bit if present; `debian/control` source/package paragraphs are present; README and UAT docs contain build/lintian/no-pyc/VM install guidance. Do not make smoke invoke package build tools by default.</action>
  <verify>
    <automated>python3 tools/smoke_phase8.py --quick</automated>
    <automated>python3 tools/smoke_phase8.py</automated>
  </verify>
  <acceptance_criteria>
    - Final smoke covers PKG-05/06/07 documentation and local no-pyc source contracts.
    - Smoke remains non-mutating and cross-host safe.
  </acceptance_criteria>
  <done>Final package verification is represented in local smoke.</done>
</task>

<task type="auto">
  <name>Task 2: Run optional Debian build and lint gates</name>
  <files>.planning/phases/08-packaging/08-HUMAN-UAT.md</files>
  <read_first>
    @debian/control
    @debian/rules
    @debian/acercontrol.install
    @.planning/phases/08-packaging/08-HUMAN-UAT.md
  </read_first>
  <action>If `dpkg-buildpackage` is available, run `dpkg-buildpackage -us -uc -b` from repo root. If a `.deb` is produced, run `lintian ../acercontrol_*.deb` if `lintian` is available and run `dpkg -c ../acercontrol_*.deb | grep '\\.pyc$'` expecting no matches. If tools are unavailable, do not fail the phase on macOS; update the UAT checklist or summary with exact skipped commands and mark them Linux-pending. Do not run `sudo apt install` automatically unless the user explicitly asks during execution.</action>
  <verify>
    <automated>python3 tools/smoke_phase8.py</automated>
  </verify>
  <acceptance_criteria>
    - Build/lintian/no-pyc results are recorded if run.
    - Missing Linux tooling is recorded as pending UAT, not silently ignored.
    - No privileged package install is run without explicit user instruction.
  </acceptance_criteria>
  <done>Package build gates are either run or explicitly deferred for Linux UAT.</done>
</task>

<task type="auto">
  <name>Task 3: Full Phase 1-8 regression pass</name>
  <files>tools/smoke_phase8.py</files>
  <read_first>
    @tools/smoke_phase1.py
    @tools/smoke_phase2.py
    @tools/smoke_phase3.py
    @tools/smoke_phase4.py
    @tools/smoke_phase5.py
    @tools/smoke_phase6.py
    @tools/smoke_phase7.py
    @tools/smoke_phase8.py
  </read_first>
  <action>Run the full source/static regression chain. If a prior phase smoke fails because of a real regression, fix the owning file. If Phase 8 smoke fails due to formatting, fix the artifact or parser without weakening package, path, Recommends, postinst, install.sh, or no-pyc contracts.</action>
  <verify>
    <automated>python3 -m py_compile tools/smoke_phase8.py acercontrol/tray.py acercontrol/gui.py</automated>
    <automated>python3 tools/smoke_phase8.py</automated>
    <automated>python3 tools/smoke_phase1.py && python3 tools/smoke_phase2.py && python3 tools/smoke_phase3.py && python3 tools/smoke_phase4.py && python3 tools/smoke_phase5.py && python3 tools/smoke_phase6.py && python3 tools/smoke_phase7.py && python3 tools/smoke_phase8.py</automated>
  </verify>
  <acceptance_criteria>
    - Full Phase 1-8 source/static smoke exits 0.
    - Phase 7 TRAY-04 remains satisfied.
    - Package source contracts remain strict.
  </acceptance_criteria>
  <done>Phase 8 source/static verification is complete.</done>
</task>

<task type="auto">
  <name>Task 4: Close Phase 8 metadata</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/phases/08-packaging/08-04-SUMMARY.md</files>
  <read_first>
    @.planning/ROADMAP.md
    @.planning/STATE.md
    @.planning/phases/08-packaging/08-HUMAN-UAT.md
  </read_first>
  <action>Create the 08-04 execution summary and update roadmap/state. Mark Phase 8 plans complete if source/static gates pass; mark package build/lintian/VM install as passed only if actually run successfully, otherwise mark as pending Linux UAT. Include next-step guidance for `$gsd-verify-work` or manual UAT if needed.</action>
  <verify>
    <automated>test -s .planning/phases/08-packaging/08-04-SUMMARY.md</automated>
    <automated>rg -n "PKG-05|PKG-06|PKG-07|lintian|dpkg-buildpackage" .planning/phases/08-packaging/08-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - Summary records all verification commands and skipped Linux-only gates.
    - ROADMAP/STATE reflect Phase 8 execution status truthfully.
    - No uncommitted production changes remain after the executor commits summary metadata.
  </acceptance_criteria>
  <done>Phase 8 is closed with accurate verification status and UAT handoff.</done>
</task>

</tasks>

<verification>
Automated:
- `python3 -m py_compile tools/smoke_phase8.py acercontrol/tray.py acercontrol/gui.py`
- `python3 tools/smoke_phase8.py`
- `python3 tools/smoke_phase1.py && python3 tools/smoke_phase2.py && python3 tools/smoke_phase3.py && python3 tools/smoke_phase4.py && python3 tools/smoke_phase5.py && python3 tools/smoke_phase6.py && python3 tools/smoke_phase7.py && python3 tools/smoke_phase8.py`

Linux-only when available:
- `dpkg-buildpackage -us -uc -b`
- `lintian ../acercontrol_*.deb`
- `dpkg -c ../acercontrol_*.deb | grep '\.pyc$'` should return no rows
</verification>

<success_criteria>
Plan 08-04 is complete when full Phase 1-8 source/static verification passes, Linux package build/lintian/no-pyc gates are either passed or explicitly recorded as pending UAT, and Phase 8 metadata accurately reflects the remaining manual install/VM checks.
</success_criteria>

