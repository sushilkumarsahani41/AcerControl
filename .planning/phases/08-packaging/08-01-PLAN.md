---
phase: 08-packaging
plan: 01
type: execute
wave: 1
depends_on: ["07-03"]
files_modified:
  - tools/smoke_phase8.py
  - pyproject.toml
autonomous: true
requirements: [PKG-01]
user_setup: []
must_haves:
  truths:
    - "Phase 8 has a source/static smoke runner before packaging files are added."
    - "pyproject.toml keeps PEP 621 + setuptools and exposes CLI, GUI, and tray console scripts."
    - "The smoke runner is safe on macOS and skips packaging-file checks until those files exist."
    - "Existing Phase 1-7 smoke gates remain green after adding the tray entry point."
  artifacts:
    - path: "tools/smoke_phase8.py"
      provides: "Phase 8 source/static packaging smoke runner"
    - path: "pyproject.toml"
      provides: "Installable console script metadata for acercontrol, acercontrol-gui, and acercontrol-tray"
---

<objective>
Create the Phase 8 packaging smoke substrate and add the final installable tray console script entry point.
</objective>

<execution_context>
@/Users/sushilkumarsahani/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sushilkumarsahani/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-packaging/08-RESEARCH.md
@.planning/phases/08-packaging/08-PATTERNS.md
@.planning/phases/08-packaging/08-VALIDATION.md
@.planning/phases/07-tray-helper-hardware-compatibility/07-03-SUMMARY.md
@pyproject.toml
@tools/smoke_phase7.py

<interfaces>
- `pyproject.toml` currently has `[project.scripts]` for `acercontrol` and `acercontrol-gui`.
- Phase 7 added `acercontrol.tray:main` and a root `acercontrol_tray.py` shim; packaged installs should expose `acercontrol-tray`.
- Phase 8 checks must be side-effect-free by default and not require Debian tooling on macOS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 8 smoke runner wave 0</name>
  <files>tools/smoke_phase8.py</files>
  <read_first>
    @tools/smoke_phase7.py
    @.planning/phases/08-packaging/08-RESEARCH.md
    @.planning/phases/08-packaging/08-PATTERNS.md
    @.planning/phases/08-packaging/08-VALIDATION.md
  </read_first>
  <action>Create `tools/smoke_phase8.py` using the same helper style as `tools/smoke_phase7.py`: project-root bootstrap, `_read()`, `_non_comment_text()`, `_contains_all()`, `_assert_no_tokens()`, `run()`, and `--quick`. Initial scenarios must check Phase 8 docs exist; `pyproject.toml` has PEP 621/setuptools metadata and existing `acercontrol` + `acercontrol-gui` scripts; future Debian/data/install checks SKIP clearly until files exist. The runner must not import `gi`, must not invoke `dpkg-buildpackage`, `lintian`, `apt`, `sudo`, `pkexec`, `systemctl`, or mutate `/usr`, `/etc`, or sysfs.</action>
  <verify>
    <automated>python3 tools/smoke_phase8.py --quick</automated>
    <automated>python3 -m py_compile tools/smoke_phase8.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tools/smoke_phase8.py --quick` exits 0 before Debian/data/install files exist.
    - The runner enforces a staged packaging contract without running package tools.
    - The runner contains no direct system mutation commands or `import gi`.
  </acceptance_criteria>
  <done>Phase 8 has a safe source/static gate before packaging implementation begins.</done>
</task>

<task type="auto">
  <name>Task 2: Add tray console script metadata</name>
  <files>pyproject.toml, tools/smoke_phase8.py</files>
  <read_first>
    @pyproject.toml
    @acercontrol/tray.py
    @acercontrol_tray.py
    @.planning/phases/07-tray-helper-hardware-compatibility/07-02-SUMMARY.md
  </read_first>
  <action>Add `acercontrol-tray = "acercontrol.tray:main"` to `[project.scripts]`. Update `tools/smoke_phase8.py` so quick mode requires all three script names: `acercontrol`, `acercontrol-gui`, and `acercontrol-tray`. Do not add runtime dependencies to `pyproject.toml`; apt dependencies belong in `debian/control` in 08-02.</action>
  <verify>
    <automated>python3 tools/smoke_phase8.py --quick</automated>
    <automated>python3 -m py_compile acercontrol/tray.py tools/smoke_phase8.py</automated>
    <automated>python3 tools/smoke_phase7.py --quick</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` remains PEP 621 + setuptools.
    - `[project.scripts]` includes CLI, GUI, and tray entry points.
    - No Python package dependency section is introduced.
  </acceptance_criteria>
  <done>Packaged installs will expose all three user-facing binaries.</done>
</task>

<task type="auto">
  <name>Task 3: 08-01 regression pass</name>
  <files>tools/smoke_phase8.py, pyproject.toml</files>
  <read_first>
    @tools/smoke_phase1.py
    @tools/smoke_phase2.py
    @tools/smoke_phase7.py
    @tools/smoke_phase8.py
  </read_first>
  <action>Run the 08-01 verification set. If smoke checks are too brittle, fix the runner without weakening the staged packaging, no-mutation, or pyproject script contracts. Do not create Debian/data/install files in this plan.</action>
  <verify>
    <automated>python3 tools/smoke_phase8.py</automated>
    <automated>python3 tools/smoke_phase1.py && python3 tools/smoke_phase2.py && python3 tools/smoke_phase7.py && python3 tools/smoke_phase8.py</automated>
  </verify>
  <acceptance_criteria>
    - Full Phase 8 smoke exits 0 with future packaging scenarios skipped.
    - Prior smoke gates affected by console-script metadata remain green.
  </acceptance_criteria>
  <done>Phase 8 planning substrate and entry points are verified.</done>
</task>

</tasks>

<verification>
Automated:
- `python3 -m py_compile tools/smoke_phase8.py acercontrol/tray.py`
- `python3 tools/smoke_phase8.py --quick`
- `python3 tools/smoke_phase8.py`
- `python3 tools/smoke_phase1.py && python3 tools/smoke_phase2.py && python3 tools/smoke_phase7.py && python3 tools/smoke_phase8.py`
</verification>

<success_criteria>
Plan 08-01 is complete when Phase 8 has a side-effect-free smoke runner and `pyproject.toml` exposes `acercontrol`, `acercontrol-gui`, and `acercontrol-tray` without adding runtime Python dependencies.
</success_criteria>

