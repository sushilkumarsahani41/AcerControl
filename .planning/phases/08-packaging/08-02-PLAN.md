---
phase: 08-packaging
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - tools/smoke_phase8.py
  - data/org.acercontrol.AcerControl.desktop
  - data/99-acer-wmi.conf
  - data/icons/hicolor/scalable/apps/org.acercontrol.AcerControl.svg
  - data/icons/hicolor/symbolic/apps/org.acercontrol.AcerControl-symbolic.svg
  - debian/control
  - debian/changelog
  - debian/copyright
  - debian/rules
  - debian/source/format
  - debian/acercontrol.install
  - debian/acercontrol.postinst
  - debian/acercontrol.postrm
autonomous: true
requirements: [PKG-02, PKG-03, PKG-04, PKG-09, PKG-10, PKG-11, TRAY-04]
user_setup: []
must_haves:
  truths:
    - "Debian packaging metadata is hand-written and buildable with debhelper + pybuild on Linux."
    - "Desktop file basename matches org.acercontrol.AcerControl and Icon uses the same app ID."
    - "Tray packages are Recommends only, never hard runtime Depends."
    - "System data installs to Debian/FHS paths without setuptools package_data."
  artifacts:
    - path: "debian/"
      provides: "Debian source package metadata and install hooks"
    - path: "data/org.acercontrol.AcerControl.desktop"
      provides: "GNOME launcher and Gio.Notification desktop identity"
    - path: "data/icons/hicolor/"
      provides: "Color and symbolic application icons"
    - path: "data/99-acer-wmi.conf"
      provides: "Persistent acer_wmi predator_v4 module option"
---

<objective>
Create Debian packaging metadata and installable desktop/system data files, with source/static gates for all PKG data contracts and the Phase 7 TRAY-04 handoff.
</objective>

<execution_context>
@/Users/sushilkumarsahani/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sushilkumarsahani/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-packaging/08-RESEARCH.md
@.planning/phases/08-packaging/08-PATTERNS.md
@.planning/phases/08-packaging/08-VALIDATION.md
@.planning/phases/07-tray-helper-hardware-compatibility/07-HUMAN-UAT.md
@tools/smoke_phase8.py
@tools/smoke_phase7.py
@data/org.acercontrol.policy
@data/acer-performance.service
@data/acer-performance@.service
@libexec/acercontrol-setprofile

<interfaces>
- Polkit policy annotations currently pin wrappers under `/usr/libexec/acercontrol/`.
- Phase 6 systemd units call `/usr/libexec/acercontrol/acercontrol-setprofile`.
- Gio.Notification requires installed desktop basename to match `org.acercontrol.AcerControl`.
- Phase 7 smoke enforces tray deps under `Recommends:` once `debian/control` exists.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand smoke for Debian/data contracts</name>
  <files>tools/smoke_phase8.py</files>
  <read_first>
    @tools/smoke_phase8.py
    @tools/smoke_phase7.py
    @.planning/phases/08-packaging/08-PATTERNS.md
    @.planning/phases/08-packaging/08-VALIDATION.md
  </read_first>
  <action>Extend `tools/smoke_phase8.py` so quick/full mode checks become strict once files exist. Add scenarios for: desktop file basename/content; color + symbolic icon paths and `currentColor`; `data/99-acer-wmi.conf`; `debian/control` Build-Depends/Depends/Recommends including TRAY-04; tray packages absent from Depends; `debian/acercontrol.install` maps wrappers, policy, units, desktop, icons, and modprobe config; `postinst` has desktop/icon/systemd hooks and initramfs/reboot guidance; `postrm` refreshes caches/daemon; `debian/rules` contains `dh $@`; `debian/source/format` is `3.0 (native)`. Keep `dpkg-buildpackage` and `lintian` out of default smoke.</action>
  <verify>
    <automated>python3 tools/smoke_phase8.py --quick</automated>
    <automated>python3 -m py_compile tools/smoke_phase8.py</automated>
  </verify>
  <acceptance_criteria>
    - Smoke still passes with new scenarios skipped until files exist.
    - Once files exist, smoke enforces all PKG data/metadata contracts.
    - Smoke remains side-effect-free.
  </acceptance_criteria>
  <done>Packaging/data expectations are executable before artifacts are added.</done>
</task>

<task type="auto">
  <name>Task 2: Add desktop, icons, and modprobe data</name>
  <files>data/org.acercontrol.AcerControl.desktop, data/99-acer-wmi.conf, data/icons/hicolor/scalable/apps/org.acercontrol.AcerControl.svg, data/icons/hicolor/symbolic/apps/org.acercontrol.AcerControl-symbolic.svg</files>
  <read_first>
    @.planning/phases/08-packaging/08-PATTERNS.md
    @.planning/research/SUMMARY.md
    @acercontrol/gui.py
  </read_first>
  <action>Create the app desktop file and icon assets. Desktop file must use `Name=AcerControl`, `Exec=acercontrol-gui`, `Icon=org.acercontrol.AcerControl`, `Terminal=false`, `Type=Application`, `Categories=System;HardwareSettings;`, and meaningful keywords. Color icon must be a scalable SVG. Symbolic icon must be a monochrome SVG using `currentColor`. Create `data/99-acer-wmi.conf` containing `options acer_wmi predator_v4=1`.</action>
  <verify>
    <automated>python3 tools/smoke_phase8.py --quick</automated>
    <automated>test -s data/org.acercontrol.AcerControl.desktop</automated>
    <automated>test -s data/icons/hicolor/scalable/apps/org.acercontrol.AcerControl.svg</automated>
    <automated>test -s data/icons/hicolor/symbolic/apps/org.acercontrol.AcerControl-symbolic.svg</automated>
  </verify>
  <acceptance_criteria>
    - Desktop basename matches application ID.
    - Icon basename matches desktop `Icon=`.
    - Symbolic icon uses `currentColor`.
    - Modprobe file contains exactly the predator_v4 option.
  </acceptance_criteria>
  <done>Desktop integration data exists and passes source checks.</done>
</task>

<task type="auto">
  <name>Task 3: Add Debian metadata and install rules</name>
  <files>debian/control, debian/changelog, debian/copyright, debian/rules, debian/source/format, debian/acercontrol.install, debian/acercontrol.postinst, debian/acercontrol.postrm</files>
  <read_first>
    @.planning/phases/08-packaging/08-RESEARCH.md
    @.planning/research/STACK.md
    @.planning/research/PITFALLS.md
    @data/org.acercontrol.policy
    @data/acer-performance.service
    @data/acer-performance@.service
  </read_first>
  <action>Create the `debian/` directory. Use `3.0 (native)`, debhelper compat 13, `dh-sequence-python3`, `pybuild-plugin-pyproject`, `python3-all`, and setuptools build deps. Runtime Depends must include `${python3:Depends}`, `${misc:Depends}`, `python3-gi`, `python3-gi-cairo`, `gir1.2-gtk-4.0`, `gir1.2-adw-1`, `policykit-1`, `systemd`, `desktop-file-utils`, and `hicolor-icon-theme`. Recommends must include `gnome-shell-extension-appindicator` and `gir1.2-ayatanaappindicator3-0.1`. `debian/acercontrol.install` must map all data and wrappers to target paths from `08-PATTERNS.md`. `postinst` must refresh desktop/icon caches and systemd daemon, and print initramfs/reboot guidance. `rules` must be executable and use `dh $@`; use shell `chmod +x debian/rules` after creating it.</action>
  <verify>
    <automated>python3 tools/smoke_phase8.py --quick</automated>
    <automated>python3 tools/smoke_phase7.py</automated>
    <automated>test -x debian/rules</automated>
  </verify>
  <acceptance_criteria>
    - `debian/control` satisfies PKG-02, PKG-03, and TRAY-04.
    - install rules include wrappers, policy, units, desktop, icons, and modprobe config.
    - postinst/postrm preserve `#DEBHELPER#`.
    - Phase 7 packaging Recommends gate no longer skips and still passes.
  </acceptance_criteria>
  <done>Debian packaging metadata and install rules are source/static verified.</done>
</task>

<task type="auto">
  <name>Task 4: 08-02 regression pass</name>
  <files>tools/smoke_phase8.py, debian/control, debian/acercontrol.install</files>
  <read_first>
    @tools/smoke_phase1.py
    @tools/smoke_phase7.py
    @tools/smoke_phase8.py
  </read_first>
  <action>Run the 08-02 verification set. If source checks fail for formatting, adjust artifacts or the parser without weakening the dependency/install/path contracts. Do not run privileged install commands in this task.</action>
  <verify>
    <automated>python3 tools/smoke_phase8.py</automated>
    <automated>python3 tools/smoke_phase1.py && python3 tools/smoke_phase7.py && python3 tools/smoke_phase8.py</automated>
  </verify>
  <acceptance_criteria>
    - Full Phase 8 smoke exits 0 for Debian/data artifacts.
    - Phase 7 TRAY-04 handoff is satisfied.
    - No prior source/static smoke gate regresses.
  </acceptance_criteria>
  <done>Debian package metadata/data plane is ready for build/install work.</done>
</task>

</tasks>

<verification>
Automated:
- `python3 -m py_compile tools/smoke_phase8.py`
- `python3 tools/smoke_phase8.py --quick`
- `python3 tools/smoke_phase8.py`
- `python3 tools/smoke_phase7.py`
- `python3 tools/smoke_phase1.py && python3 tools/smoke_phase7.py && python3 tools/smoke_phase8.py`
</verification>

<success_criteria>
Plan 08-02 is complete when Debian metadata, install rules, desktop file, icons, and modprobe config exist, Phase 8 smoke validates them, and Phase 7's tray dependency packaging check passes against `debian/control`.
</success_criteria>

