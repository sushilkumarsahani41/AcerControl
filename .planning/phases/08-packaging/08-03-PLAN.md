---
phase: 08-packaging
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - tools/smoke_phase8.py
  - install.sh
  - README.md
  - .planning/phases/08-packaging/08-HUMAN-UAT.md
autonomous: true
requirements: [PKG-08, PKG-11]
user_setup: []
must_haves:
  truths:
    - "Manual fallback install is explicit, root-aware, and does not use network package managers for Python code."
    - "install.sh preserves the same privilege boundary by installing wrappers where the polkit policy expects them."
    - "README and UAT distinguish source/static checks from Linux-only packaging/install validation."
    - "Fallback install provides CLI, GUI, tray, wrappers, policy, systemd units, desktop file, icons, and modprobe config."
  artifacts:
    - path: "install.sh"
      provides: "Manual/non-Debian install fallback"
    - path: "README.md"
      provides: "User-facing install/build guidance"
    - path: ".planning/phases/08-packaging/08-HUMAN-UAT.md"
      provides: "Package and fallback install UAT checklist"
---

<objective>
Add the manual install fallback and user-facing package/UAT documentation needed to validate Phase 8 outside the Debian build path.
</objective>

<execution_context>
@/Users/sushilkumarsahani/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sushilkumarsahani/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-packaging/08-RESEARCH.md
@.planning/phases/08-packaging/08-PATTERNS.md
@.planning/phases/08-packaging/08-VALIDATION.md
@.planning/phases/07-tray-helper-hardware-compatibility/07-HUMAN-UAT.md
@tools/bundle_cli.py
@tools/smoke_phase8.py
@README.md
@pyproject.toml

<interfaces>
- `tools/bundle_cli.py` produces `dist/acercontrol`, the standalone zero-GTK CLI.
- `acercontrol.privilege.resolve_wrapper()` checks `/usr/libexec/acercontrol` before `/usr/local/libexec/acercontrol`.
- `data/org.acercontrol.policy` pins wrapper paths to `/usr/libexec/acercontrol/...`; fallback install must install wrappers there for named polkit actions to apply.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand smoke for fallback installer and docs</name>
  <files>tools/smoke_phase8.py</files>
  <read_first>
    @tools/smoke_phase8.py
    @.planning/phases/08-packaging/08-PATTERNS.md
    @.planning/phases/08-packaging/08-VALIDATION.md
  </read_first>
  <action>Extend `tools/smoke_phase8.py` with staged checks for `install.sh`, `README.md`, and `08-HUMAN-UAT.md`. The install script checks must require tokens for `tools/bundle_cli.py`, `dist/acercontrol`, `/usr/local/bin/acercontrol`, `/usr/local/bin/acercontrol-gui`, `/usr/local/bin/acercontrol-tray`, `/usr/libexec/acercontrol`, `/usr/share/polkit-1/actions`, `/etc/systemd/system`, `/usr/share/applications`, `/usr/share/icons/hicolor`, `/etc/modprobe.d/99-acer-wmi.conf`, `systemctl daemon-reload`, `update-desktop-database`, `gtk-update-icon-cache`, `update-initramfs -u`, and reboot guidance. Forbid `curl`, `wget`, and `pip install`. Keep checks skipped until files exist.</action>
  <verify>
    <automated>python3 tools/smoke_phase8.py --quick</automated>
    <automated>python3 -m py_compile tools/smoke_phase8.py</automated>
  </verify>
  <acceptance_criteria>
    - Fallback installer requirements are executable before implementation.
    - Smoke forbids network-based install shortcuts.
    - Smoke remains side-effect-free.
  </acceptance_criteria>
  <done>Fallback installer and docs expectations are source-gated.</done>
</task>

<task type="auto">
  <name>Task 2: Add install.sh fallback</name>
  <files>install.sh</files>
  <read_first>
    @tools/bundle_cli.py
    @data/org.acercontrol.policy
    @data/acer-performance.service
    @debian/acercontrol.install
    @.planning/phases/08-packaging/08-RESEARCH.md
  </read_first>
  <action>Create executable `install.sh` with `set -euo pipefail`. It should require root or re-exec via `sudo` for installation, run `python3 tools/bundle_cli.py`, copy `dist/acercontrol` to `/usr/local/bin/acercontrol`, install GUI/tray launcher scripts under `/usr/local/bin/` that set `PYTHONPATH=/usr/local/share/acercontrol` and run `python3 -m acercontrol.gui` / `python3 -m acercontrol.tray`, copy the package to `/usr/local/share/acercontrol/acercontrol`, copy wrappers to `/usr/libexec/acercontrol`, copy policy to `/usr/share/polkit-1/actions/`, copy systemd units to `/etc/systemd/system/`, copy desktop/icons to `/usr/share/...`, copy modprobe config to `/etc/modprobe.d/99-acer-wmi.conf`, refresh daemon/desktop/icon caches, run `update-initramfs -u` if available, and print reboot guidance. It may support `--dry-run` if useful, but actual install behavior must be concrete.</action>
  <verify>
    <automated>test -x install.sh</automated>
    <automated>python3 tools/smoke_phase8.py --quick</automated>
    <automated>python3 tools/smoke_phase2.py</automated>
  </verify>
  <acceptance_criteria>
    - `install.sh` is executable.
    - Fallback install path preserves named polkit action compatibility by installing wrappers to `/usr/libexec/acercontrol`.
    - No network install or pip dependency install is introduced.
    - CLI bundler path remains GTK-free.
  </acceptance_criteria>
  <done>Manual fallback install script exists and is source/static verified.</done>
</task>

<task type="auto">
  <name>Task 3: Update README and add Phase 8 UAT checklist</name>
  <files>README.md, .planning/phases/08-packaging/08-HUMAN-UAT.md</files>
  <read_first>
    @README.md
    @.planning/phases/08-packaging/08-VALIDATION.md
    @.planning/phases/07-tray-helper-hardware-compatibility/07-HUMAN-UAT.md
  </read_first>
  <action>Update `README.md` with concise build/install instructions: apt runtime deps, packaging build deps, `.deb` build/install flow, manual `install.sh` fallback, reboot/initramfs note, and the three commands `acercontrol`, `acercontrol-gui`, `acercontrol-tray`. Create `08-HUMAN-UAT.md` with exact Linux checks for `dpkg-buildpackage`, `lintian`, no `.pyc`, clean VM install, launcher/icon visibility, polkit action path, wrappers, systemd units, modprobe config, and fallback installer. Mark hardware/Linux commands as manual UAT, not local smoke gates.</action>
  <verify>
    <automated>test -s .planning/phases/08-packaging/08-HUMAN-UAT.md</automated>
    <automated>rg -n "dpkg-buildpackage|lintian|install.sh|update-initramfs|acercontrol-tray" README.md .planning/phases/08-packaging/08-HUMAN-UAT.md</automated>
    <automated>python3 tools/smoke_phase8.py --quick</automated>
  </verify>
  <acceptance_criteria>
    - README contains package and fallback install paths.
    - UAT checklist covers PKG-05 through PKG-08 and desktop/icon/polkit/systemd checks.
    - Documentation explicitly says reboot is needed after initramfs update for `predator_v4=1`.
  </acceptance_criteria>
  <done>Users and testers have package/fallback install instructions.</done>
</task>

<task type="auto">
  <name>Task 4: 08-03 regression pass</name>
  <files>tools/smoke_phase8.py, install.sh, README.md</files>
  <read_first>
    @tools/smoke_phase2.py
    @tools/smoke_phase7.py
    @tools/smoke_phase8.py
  </read_first>
  <action>Run the 08-03 verification set. If the installer source checks fail because of harmless formatting, adjust the script or smoke runner without weakening path, cache refresh, initramfs, or no-network contracts. Do not run actual system install commands on this host.</action>
  <verify>
    <automated>python3 tools/smoke_phase8.py</automated>
    <automated>python3 tools/smoke_phase2.py && python3 tools/smoke_phase7.py && python3 tools/smoke_phase8.py</automated>
  </verify>
  <acceptance_criteria>
    - Full Phase 8 smoke exits 0 with install/docs checks.
    - CLI bundler and tray packaging gates remain green.
  </acceptance_criteria>
  <done>Manual fallback and user-facing install docs are ready for final package build verification.</done>
</task>

</tasks>

<verification>
Automated:
- `python3 -m py_compile tools/smoke_phase8.py`
- `test -x install.sh`
- `python3 tools/smoke_phase8.py --quick`
- `python3 tools/smoke_phase8.py`
- `python3 tools/smoke_phase2.py && python3 tools/smoke_phase7.py && python3 tools/smoke_phase8.py`
</verification>

<success_criteria>
Plan 08-03 is complete when `install.sh`, README guidance, and Phase 8 UAT docs exist and source/static checks prove the manual fallback covers binaries, package code, wrappers, policy, units, desktop, icons, modprobe config, cache refreshes, and initramfs/reboot guidance without network install shortcuts.
</success_criteria>

