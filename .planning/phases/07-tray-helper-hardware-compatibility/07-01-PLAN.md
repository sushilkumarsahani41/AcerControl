---
phase: 07-tray-helper-hardware-compatibility
plan: 01
type: execute
wave: 1
depends_on: ["06-03"]
files_modified:
  - tools/smoke_phase7.py
  - acercontrol/tray_status.py
  - acercontrol/gui_about.py
autonomous: true
requirements: [TRAY-02]
user_setup: []
must_haves:
  truths:
    - "Tray availability detection is GTK-version-neutral and safe when the session bus or AppIndicator extension is absent."
    - "The main GTK4 GUI may report tray availability but never imports the GTK3 tray helper."
    - "Phase 7 smoke starts side-effect-free and can run on macOS without GTK typelibs."
    - "The CLI bundler remains GTK-free and does not include tray modules."
  artifacts:
    - path: "tools/smoke_phase7.py"
      provides: "Source/static and fixture smoke runner for Phase 7 tray and hardware compatibility contracts"
    - path: "acercontrol/tray_status.py"
      provides: "StatusNotifierWatcher availability helper shared by tray process and About diagnostics"
    - path: "acercontrol/gui_about.py"
      provides: "About diagnostics include tray availability without importing GTK3 tray code"
---

<objective>
Create the Phase 7 tray availability substrate: a source/static smoke runner, a safe StatusNotifierWatcher detector, and About diagnostics that mention tray availability without loading GTK3 tray code in the GTK4 GUI process.
</objective>

<execution_context>
@/Users/sushilkumarsahani/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sushilkumarsahani/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-tray-helper-hardware-compatibility/07-RESEARCH.md
@.planning/phases/07-tray-helper-hardware-compatibility/07-UI-SPEC.md
@.planning/phases/07-tray-helper-hardware-compatibility/07-PATTERNS.md
@.planning/phases/07-tray-helper-hardware-compatibility/07-VALIDATION.md
@.planning/phases/06-boot-persistence-suspend-resume/06-03-SUMMARY.md
@acercontrol/gui_about.py
@tools/bundle_cli.py
@tools/smoke_phase6.py

<interfaces>
- `acercontrol.gui_about.build_about_dialog()` serializes diagnostics through `set_debug_info()`.
- `tools/bundle_cli.py` has a fixed `BUNDLE_ORDER`; tray modules must not be added to it.
- Phase 7 checks must be safe on this macOS host where GTK typelibs may be unavailable.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase 7 smoke runner wave 0</name>
  <files>tools/smoke_phase7.py</files>
  <read_first>
    @tools/smoke_phase6.py
    @tools/smoke_phase5.py
    @.planning/phases/07-tray-helper-hardware-compatibility/07-RESEARCH.md
    @.planning/phases/07-tray-helper-hardware-compatibility/07-UI-SPEC.md
    @.planning/phases/07-tray-helper-hardware-compatibility/07-PATTERNS.md
  </read_first>
  <action>Create `tools/smoke_phase7.py` using the same project-root bootstrap, `run()` helper, `_non_comment_text()` helper, `--quick` mode, inline scenario functions, and pass-count pattern as `tools/smoke_phase6.py`. Quick mode must pass before Phase 7 implementation files exist by SKIPing implementation-file-specific scenarios. Initial scenarios must check: Phase 7 docs exist; `tools/bundle_cli.py` `BUNDLE_ORDER` does not reference tray modules; GTK4 GUI modules (`gui.py`, `gui_window.py`, `gui_about.py`) do not import `acercontrol.tray`; `tray_status.py` contract if it exists; About diagnostics tray status if wired; `tray.py` helper contract if it exists; hardware fixture checks are SKIPed until implemented; packaging Recommends contract SKIPs when `debian/control` does not exist. The runner itself must not import `gi`, must not invoke system bus commands, must not call `pkexec`, `sudo`, `systemctl`, or write sysfs.</action>
  <verify>
    <automated>python3 tools/smoke_phase7.py --quick</automated>
    <automated>python3 -m py_compile tools/smoke_phase7.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tools/smoke_phase7.py --quick` exits 0 before tray files exist.
    - The runner enforces GTK3/GTK4 process isolation and CLI bundler isolation.
    - The runner contains no `import gi`, no direct elevation tokens, no mutating system commands, and no sysfs writes.
  </acceptance_criteria>
  <done>Phase 7 has a side-effect-free source/static gate before implementation begins.</done>
</task>

<task type="auto">
  <name>Task 2: Add tray availability detector</name>
  <files>acercontrol/tray_status.py</files>
  <read_first>
    @.planning/phases/07-tray-helper-hardware-compatibility/07-RESEARCH.md
    @.planning/phases/07-tray-helper-hardware-compatibility/07-PATTERNS.md
  </read_first>
  <action>Create `acercontrol/tray_status.py` as a GTK-version-neutral helper. It may import `gi` for Gio only, but it must not require Gtk, Adw, or Ayatana. Provide a small public API such as `tray_status() -> str` and `tray_status_detail() -> dict[str, str]`. It must query the session bus using `Gio.BusType.SESSION` and `org.freedesktop.DBus.NameHasOwner` for `org.kde.StatusNotifierWatcher`. Return stable strings including `available`, `missing-watcher`, `no-session-bus`, and `unknown`. Catch `ImportError`, `ValueError`, `GLib.Error`, `AttributeError`, and `TypeError`; never raise on macOS or missing session bus.</action>
  <verify>
    <automated>python3 -m py_compile acercontrol/tray_status.py</automated>
    <automated>python3 tools/smoke_phase7.py --quick</automated>
  </verify>
  <acceptance_criteria>
    - `tray_status.py` contains `Gio.BusType.SESSION`, `NameHasOwner`, and `org.kde.StatusNotifierWatcher`.
    - It does not contain `gi.require_version("Gtk"` or `AyatanaAppIndicator3`.
    - It returns safe statuses instead of raising for missing Gio/session bus.
  </acceptance_criteria>
  <done>Tray availability can be queried without loading GTK3 or constructing an indicator.</done>
</task>

<task type="auto">
  <name>Task 3: Add tray status to About diagnostics</name>
  <files>acercontrol/gui_about.py, tools/smoke_phase7.py</files>
  <read_first>
    @acercontrol/gui_about.py
    @acercontrol/tray_status.py
    @.planning/phases/07-tray-helper-hardware-compatibility/07-UI-SPEC.md
  </read_first>
  <action>Modify `acercontrol/gui_about.py` so `_report_to_dict()` includes tray availability diagnostics from `acercontrol.tray_status`, for example a top-level `"tray": tray_status_detail()` key. Import only `tray_status` / `tray_status_detail`; do not import `acercontrol.tray`. Update `tools/smoke_phase7.py` so quick mode checks `gui_about.py` includes tray status diagnostics and still has no GTK3/Ayatana imports. The About dialog must continue to use `set_debug_info()` and preserve existing feature report fields.</action>
  <verify>
    <automated>python3 -m py_compile acercontrol/gui_about.py acercontrol/tray_status.py tools/smoke_phase7.py</automated>
    <automated>python3 tools/smoke_phase7.py --quick</automated>
    <automated>python3 tools/smoke_phase3.py</automated>
  </verify>
  <acceptance_criteria>
    - About diagnostics include a tray availability field.
    - GTK4 GUI code does not import `acercontrol.tray` and does not require GTK3.
    - Phase 3 smoke remains green.
  </acceptance_criteria>
  <done>The main GUI can report tray availability without coupling to the tray helper process.</done>
</task>

<task type="auto">
  <name>Task 4: 07-01 regression pass</name>
  <files>tools/smoke_phase7.py</files>
  <read_first>
    @tools/smoke_phase1.py
    @tools/smoke_phase2.py
    @tools/smoke_phase3.py
    @tools/smoke_phase7.py
  </read_first>
  <action>Run the 07-01 verification set. If source-token checks are too brittle, fix the smoke runner without weakening the GTK3/GTK4 isolation, bundler isolation, or About diagnostics contracts. Do not change production behavior outside `tray_status.py` and `gui_about.py` for this plan.</action>
  <verify>
    <automated>python3 tools/smoke_phase7.py</automated>
    <automated>python3 tools/smoke_phase1.py && python3 tools/smoke_phase2.py && python3 tools/smoke_phase3.py && python3 tools/smoke_phase7.py</automated>
  </verify>
  <acceptance_criteria>
    - Full Phase 7 smoke exits 0 for 07-01 implemented artifacts.
    - Existing Phase 1-3 smoke gates remain green.
  </acceptance_criteria>
  <done>Tray availability substrate is source/static verified and ready for the helper process.</done>
</task>

</tasks>

<verification>
Automated:
- `python3 -m py_compile tools/smoke_phase7.py acercontrol/tray_status.py acercontrol/gui_about.py`
- `python3 tools/smoke_phase7.py --quick`
- `python3 tools/smoke_phase7.py`
- `python3 tools/smoke_phase1.py && python3 tools/smoke_phase2.py && python3 tools/smoke_phase3.py && python3 tools/smoke_phase7.py`
</verification>

<success_criteria>
Plan 07-01 is complete when tray availability detection is available without loading GTK3, About diagnostics report the tray state, the CLI bundler remains isolated from tray code, and the Phase 7 smoke runner is ready for subsequent tray/helper checks.
</success_criteria>
