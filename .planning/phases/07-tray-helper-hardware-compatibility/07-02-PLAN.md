---
phase: 07-tray-helper-hardware-compatibility
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - tools/smoke_phase7.py
  - acercontrol/tray.py
  - acercontrol_tray.py
autonomous: true
requirements: [TRAY-01, TRAY-03]
user_setup: []
must_haves:
  truths:
    - "The tray is a separate GTK3/Ayatana helper process and is never imported by the GTK4 GUI."
    - "Tray quick-switch uses the existing privileged setprofile wrapper through run_privileged."
    - "Tray menu labels use only user-facing profile names."
    - "Show AcerControl activates the existing GUI entry point without importing GTK4 modules into the tray process."
  artifacts:
    - path: "acercontrol/tray.py"
      provides: "GTK3 + Ayatana AppIndicator helper with profile polling, quick-switch menu, Show AcerControl, and Quit"
    - path: "acercontrol_tray.py"
      provides: "Development/manual executable shim for acercontrol-tray before Phase 8 console scripts"
    - path: "tools/smoke_phase7.py"
      provides: "Strict source checks for tray helper architecture and menu behavior"
---

<objective>
Implement `acercontrol-tray` as a separate GTK3 + Ayatana helper process that reflects current profile state, offers profile quick-switch actions, and raises the main GUI through the existing single-instance application.
</objective>

<execution_context>
@/Users/sushilkumarsahani/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sushilkumarsahani/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-tray-helper-hardware-compatibility/07-RESEARCH.md
@.planning/phases/07-tray-helper-hardware-compatibility/07-UI-SPEC.md
@.planning/phases/07-tray-helper-hardware-compatibility/07-PATTERNS.md
@.planning/phases/07-tray-helper-hardware-compatibility/07-VALIDATION.md
@.planning/phases/07-tray-helper-hardware-compatibility/07-01-PLAN.md
@acercontrol/tray_status.py
@acercontrol/profiles.py
@acercontrol/core.py
@acercontrol/privilege.py
@acercontrol/gui.py
@tools/smoke_phase7.py

<interfaces>
- `read_profile()` returns `Profile`.
- `list_available_profiles()` returns supported `Profile` values or an empty list when unknown.
- `run_privileged(["acercontrol-setprofile", PROFILES[name]])` performs profile writes.
- `acercontrol.gui` handles single-instance activation and focuses the existing window on second launch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand smoke for tray helper source contract</name>
  <files>tools/smoke_phase7.py</files>
  <read_first>
    @tools/smoke_phase7.py
    @.planning/phases/07-tray-helper-hardware-compatibility/07-UI-SPEC.md
    @.planning/phases/07-tray-helper-hardware-compatibility/07-PATTERNS.md
  </read_first>
  <action>Extend `tools/smoke_phase7.py` so quick mode SKIPs `acercontrol/tray.py` checks until it exists, then strictly checks that it contains `gi.require_version("Gtk", "3.0")`, `gi.require_version("AyatanaAppIndicator3", "0.1")`, `Gtk.Menu`, `Gtk.MenuItem`, `GLib.timeout_add_seconds(2,`, `read_profile`, `list_available_profiles`, `run_privileged`, `acercontrol-setprofile`, `PROFILES[`, `Show AcerControl`, `Quit`, and the five user-facing profile names in order. Add forbidden checks for `Gtk 4.0`, `Adw`, `Gtk.StatusIcon`, `AppIndicator3` legacy naming without `Ayatana`, direct `pkexec`, direct `sudo`, direct `systemctl`, `shell=True`, and direct `/sys/firmware/acpi/platform_profile` writes. Add checks that `acercontrol_tray.py` imports only `acercontrol.tray.main` once it exists.</action>
  <verify>
    <automated>python3 tools/smoke_phase7.py --quick</automated>
  </verify>
  <acceptance_criteria>
    - Smoke runner enforces tray process isolation and wrapper-only mutation.
    - Smoke runner enforces menu copy/order and Show/Quit entries.
    - Quick mode still passes before `tray.py` exists.
  </acceptance_criteria>
  <done>Tray helper expectations are executable before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Add GTK3 Ayatana tray helper</name>
  <files>acercontrol/tray.py</files>
  <read_first>
    @acercontrol/tray_status.py
    @acercontrol/core.py
    @acercontrol/profiles.py
    @acercontrol/privilege.py
    @.planning/phases/07-tray-helper-hardware-compatibility/07-UI-SPEC.md
  </read_first>
  <action>Create `acercontrol/tray.py`. The module must define `PROFILE_ORDER = ("eco", "quiet", "balanced", "performance", "turbo")` and a `main() -> int`. Before importing GTK3/Ayatana, call `tray_status()` and exit 0 with one concise message if unavailable. Lazily import GTK3/Ayatana inside the tray code with `gi.require_version("Gtk", "3.0")` and `gi.require_version("AyatanaAppIndicator3", "0.1")`, catching `ImportError` and `ValueError` to exit 0 when optional dependencies are not installed. Build an Ayatana AppIndicator with baseline app icon name `org.acercontrol.AcerControl`, a `Gtk.Menu`, five profile items, separator, `Show AcerControl`, and `Quit`. Refresh current profile with `GLib.timeout_add_seconds(2, ...)`; mark active profile where possible and set unsupported profile items insensitive based on `list_available_profiles()`. Do not import `acercontrol.gui` and do not call privileged commands directly.</action>
  <verify>
    <automated>python3 -m py_compile acercontrol/tray.py</automated>
    <automated>python3 tools/smoke_phase7.py --quick</automated>
  </verify>
  <acceptance_criteria>
    - Tray helper exits cleanly when watcher or optional typelib is unavailable.
    - Tray helper is the only source that requires GTK3/Ayatana.
    - Menu labels are user-facing only.
    - Polling uses `GLib.timeout_add_seconds(2, ...)`, not a thread.
  </acceptance_criteria>
  <done>The separate tray helper process can construct and maintain an AppIndicator menu.</done>
</task>

<task type="auto">
  <name>Task 3: Add quick-switch and Show AcerControl actions</name>
  <files>acercontrol/tray.py, acercontrol_tray.py</files>
  <read_first>
    @acercontrol/tray.py
    @acercontrol/gui.py
    @acercontrol/privilege.py
    @.planning/phases/07-tray-helper-hardware-compatibility/07-UI-SPEC.md
  </read_first>
  <action>Complete tray action behavior. Each profile menu item must call `run_privileged(["acercontrol-setprofile", PROFILES[profile_name]])`, handle cancellation/failure without traceback, and refresh read-back state after success. `Show AcerControl` must launch `acercontrol-gui` when found in PATH and fall back to `[sys.executable, "-m", "acercontrol.gui"]` for development; it must not import `acercontrol.gui`. Add root-level shim `acercontrol_tray.py` with shebang and only `from acercontrol.tray import main` plus `raise SystemExit(main())`, so manual execution is possible before Phase 8 packaging adds console scripts. `Quit` must stop the GTK main loop.</action>
  <verify>
    <automated>python3 -m py_compile acercontrol/tray.py acercontrol_tray.py</automated>
    <automated>python3 tools/smoke_phase7.py --quick</automated>
    <automated>python3 tools/smoke_phase2.py</automated>
  </verify>
  <acceptance_criteria>
    - Quick-switch uses `run_privileged(["acercontrol-setprofile", PROFILES[` and never raw kernel labels in menu text.
    - Show action activates/launches GUI without importing GTK4.
    - Shim exists for `acercontrol-tray` development/manual runs.
    - Phase 2 bundler smoke remains green.
  </acceptance_criteria>
  <done>The tray helper exposes all required menu actions without weakening the CLI or GTK process boundaries.</done>
</task>

<task type="auto">
  <name>Task 4: 07-02 regression pass</name>
  <files>tools/smoke_phase7.py, acercontrol/tray.py</files>
  <read_first>
    @tools/smoke_phase7.py
    @tools/smoke_phase1.py
    @tools/smoke_phase2.py
    @tools/smoke_phase3.py
  </read_first>
  <action>Run the 07-02 verification set. If static token checks fail due to harmless formatting, fix the source formatting or smoke runner without changing behavior. Do not weaken the process-isolation or wrapper-only checks.</action>
  <verify>
    <automated>python3 tools/smoke_phase7.py</automated>
    <automated>python3 tools/smoke_phase1.py && python3 tools/smoke_phase2.py && python3 tools/smoke_phase3.py && python3 tools/smoke_phase7.py</automated>
  </verify>
  <acceptance_criteria>
    - Full Phase 7 smoke exits 0 for tray helper behavior.
    - Existing Phase 1-3 smoke gates remain green.
  </acceptance_criteria>
  <done>Tray helper implementation is source/static verified and ready for hardware compatibility closure.</done>
</task>

</tasks>

<verification>
Automated:
- `python3 -m py_compile acercontrol/tray.py acercontrol_tray.py tools/smoke_phase7.py`
- `python3 tools/smoke_phase7.py --quick`
- `python3 tools/smoke_phase7.py`
- `python3 tools/smoke_phase1.py && python3 tools/smoke_phase2.py && python3 tools/smoke_phase3.py && python3 tools/smoke_phase7.py`

Manual Ubuntu UAT after implementation:
- With AppIndicator extension enabled, run `python3 acercontrol_tray.py`; tray menu appears with five profile items, Show AcerControl, and Quit.
- Switch each profile from tray; `acercontrol get` reports the requested user-facing name.
- Use Show AcerControl while GUI is closed and while already open; it launches or focuses the existing window.
</verification>

<success_criteria>
Plan 07-02 is complete when a separate GTK3/Ayatana tray helper exists, reflects profile state, offers wrapper-backed quick-switch actions, raises the GUI without importing GTK4, and leaves Phase 1-3 plus Phase 7 smoke gates green.
</success_criteria>
