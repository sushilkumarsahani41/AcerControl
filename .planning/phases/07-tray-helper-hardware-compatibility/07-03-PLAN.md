---
phase: 07-tray-helper-hardware-compatibility
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - tools/smoke_phase7.py
  - acercontrol/core.py
  - acercontrol/sysfs.py
  - acercontrol/gui_profiles.py
  - acercontrol/gui_sensors.py
  - acercontrol/features.py
  - .planning/phases/07-tray-helper-hardware-compatibility/07-HUMAN-UAT.md
autonomous: true
requirements: [TRAY-04, HW-01, HW-02]
user_setup: []
must_haves:
  truths:
    - "Phase 7 ends with executable fixture coverage for partial sensors/profile choices and a human UAT checklist for PHN16-72."
    - "TRAY-04 is protected by a source/static packaging contract even though Phase 8 owns creating debian/control."
    - "Hardware compatibility broadens graceful degradation inside predator_v4 support only; classic acer_wmi remains out of scope."
    - "No regression is allowed in Phase 1-7 smoke."
  artifacts:
    - path: "tools/smoke_phase7.py"
      provides: "Full Phase 7 smoke including tray package contract and hardware compatibility fixtures"
    - path: ".planning/phases/07-tray-helper-hardware-compatibility/07-HUMAN-UAT.md"
      provides: "Manual PHN16-72 and compatible-hardware UAT checklist"
---

<objective>
Close Phase 7 with hardware compatibility fixture gates, a packaging Recommends handoff check for tray dependencies, a human UAT checklist, and the full Phase 1-7 regression pass.
</objective>

<execution_context>
@/Users/sushilkumarsahani/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sushilkumarsahani/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-tray-helper-hardware-compatibility/07-RESEARCH.md
@.planning/phases/07-tray-helper-hardware-compatibility/07-UI-SPEC.md
@.planning/phases/07-tray-helper-hardware-compatibility/07-PATTERNS.md
@.planning/phases/07-tray-helper-hardware-compatibility/07-VALIDATION.md
@.planning/phases/07-tray-helper-hardware-compatibility/07-02-PLAN.md
@acercontrol/core.py
@acercontrol/sysfs.py
@acercontrol/gui_profiles.py
@acercontrol/gui_sensors.py
@acercontrol/features.py
@tools/smoke_phase7.py

<interfaces>
- `sysfs.find_hwmon()` accepts monkeypatched `HWMON_BASE` inside a Python process and uses file presence/counting.
- `read_acer_sensors()` returns `None` for missing fan/temp files.
- `available_profiles()` reads a supplied choices path and filters by kernel values.
- Phase 8 will create `debian/control`; Phase 7 smoke can only enforce the contract when that file exists.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand smoke for hardware compatibility and packaging handoff</name>
  <files>tools/smoke_phase7.py</files>
  <read_first>
    @tools/smoke_phase7.py
    @tools/smoke_phase1.py
    @.planning/phases/07-tray-helper-hardware-compatibility/07-RESEARCH.md
    @.planning/phases/07-tray-helper-hardware-compatibility/07-VALIDATION.md
  </read_first>
  <action>Extend `tools/smoke_phase7.py` full mode with tempdir fixture checks for partial-compatible hardware: duplicate `acer` hwmon entries where most-populated wins; an `acer` hwmon containing `fan1_input`, `temp1_input`, and `temp2_input` but missing `fan2_input` and `temp3_input`, asserting `read_acer_sensors()` returns `None` for missing values; a temporary `platform_profile_choices` file missing at least one known kernel value, asserting `available_profiles()` excludes it without raising. Add a packaging contract scenario: if `debian/control` is absent, SKIP with a Phase 8 handoff message; if present, assert `gir1.2-ayatanaappindicator3-0.1` and `gnome-shell-extension-appindicator` appear under `Recommends:` and not under runtime `Depends:`. Keep quick mode focused on source checks; full mode runs fixtures.</action>
  <verify>
    <automated>python3 tools/smoke_phase7.py --quick</automated>
    <automated>python3 tools/smoke_phase7.py</automated>
  </verify>
  <acceptance_criteria>
    - Full smoke fixture tests prove partial sensor/profile-choice behavior.
    - Packaging contract is enforced when `debian/control` exists and skipped clearly when absent.
    - No production files are changed in this task unless a fixture exposes a real compatibility bug.
  </acceptance_criteria>
  <done>Hardware compatibility and TRAY-04 handoff are executable smoke contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Fix any compatibility gaps found by fixtures</name>
  <files>acercontrol/core.py, acercontrol/sysfs.py, acercontrol/gui_profiles.py, acercontrol/gui_sensors.py, acercontrol/features.py</files>
  <read_first>
    @tools/smoke_phase7.py
    @acercontrol/core.py
    @acercontrol/sysfs.py
    @acercontrol/gui_profiles.py
    @acercontrol/gui_sensors.py
    @acercontrol/features.py
  </read_first>
  <action>Run the full Phase 7 smoke after Task 1. If it exposes a real compatibility gap, make the smallest scoped fix in the owning module. Examples of acceptable fixes: invalidate hwmon cache during fixture setup; ensure missing fan/temp values remain `None`; preserve unavailable profile button behavior; ensure sensor placeholder text remains stable. Do not broaden official v1 support beyond predator_v4 and do not change core profile mapping. If no gap is found, make no production code changes and record the no-op in the plan summary.</action>
  <verify>
    <automated>python3 tools/smoke_phase7.py</automated>
    <automated>python3 tools/smoke_phase1.py && python3 tools/smoke_phase5.py && python3 tools/smoke_phase7.py</automated>
  </verify>
  <acceptance_criteria>
    - Fixture checks pass.
    - Existing profile/sensor behavior remains source-compatible with Phases 4 and 5.
    - No unrelated refactors are introduced.
  </acceptance_criteria>
  <done>Partial hardware compatibility behavior is either proven or fixed.</done>
</task>

<task type="auto">
  <name>Task 3: Add Phase 7 human UAT checklist</name>
  <files>.planning/phases/07-tray-helper-hardware-compatibility/07-HUMAN-UAT.md</files>
  <read_first>
    @.planning/phases/07-tray-helper-hardware-compatibility/07-VALIDATION.md
    @.planning/phases/06-boot-persistence-suspend-resume/06-03-SUMMARY.md
    @.planning/phases/05-live-sensors-notifications/05-01-SUMMARY.md
  </read_first>
  <action>Create `07-HUMAN-UAT.md` with explicit Ubuntu/PHN16-72 manual checks: install optional tray dependencies; verify AppIndicator-present flow; verify watcher-absent exit 0 flow; verify tray quick-switch for all five profiles; verify Show AcerControl focuses/launches the GUI; verify full PHN16-72 happy path from profile buttons through sensors, boot persistence, suspend/resume, and tray; verify compatible partial-sensor behavior or document fixture-equivalent coverage when second hardware is unavailable. Include exact commands where useful, but mark hardware-only commands as manual UAT, not automated gates.</action>
  <verify>
    <automated>test -s .planning/phases/07-tray-helper-hardware-compatibility/07-HUMAN-UAT.md</automated>
    <automated>rg -n "AppIndicator|StatusNotifierWatcher|PHN16-72|partial" .planning/phases/07-tray-helper-hardware-compatibility/07-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - UAT checklist covers TRAY-01..03 and HW-01..02 live validation.
    - It explicitly calls out TRAY-04 as Phase 8 packaging verification.
    - It distinguishes manual Linux/hardware UAT from local source/static verification.
  </acceptance_criteria>
  <done>Human validation for tray and hardware compatibility is ready before Phase 7 closeout.</done>
</task>

<task type="auto">
  <name>Task 4: Final Phase 7 regression pass</name>
  <files>tools/smoke_phase7.py</files>
  <read_first>
    @tools/smoke_phase1.py
    @tools/smoke_phase2.py
    @tools/smoke_phase3.py
    @tools/smoke_phase4.py
    @tools/smoke_phase5.py
    @tools/smoke_phase6.py
    @tools/smoke_phase7.py
  </read_first>
  <action>Run the full Phase 1-7 smoke chain. If Phase 7 full smoke finds source-token formatting failures, fix formatting without changing behavior. If earlier phase smoke fails due to a real regression, fix the regression in the owning file. Do not weaken previous phase gates.</action>
  <verify>
    <automated>python3 -m py_compile tools/smoke_phase7.py acercontrol/tray_status.py acercontrol/tray.py acercontrol_tray.py acercontrol/gui_about.py</automated>
    <automated>python3 tools/smoke_phase7.py</automated>
    <automated>python3 tools/smoke_phase1.py && python3 tools/smoke_phase2.py && python3 tools/smoke_phase3.py && python3 tools/smoke_phase4.py && python3 tools/smoke_phase5.py && python3 tools/smoke_phase6.py && python3 tools/smoke_phase7.py</automated>
  </verify>
  <acceptance_criteria>
    - Full Phase 7 smoke exits 0.
    - Full Phase 1-6 smoke remains green.
    - No direct sysfs writes, direct pkexec/sudo, arbitrary service control, GTK3-in-GTK4 imports, or tray modules in CLI bundle are introduced.
  </acceptance_criteria>
  <done>Phase 7 implementation is source/static verified and ready for Ubuntu tray and hardware UAT.</done>
</task>

</tasks>

<verification>
Automated:
- `python3 -m py_compile tools/smoke_phase7.py acercontrol/tray_status.py acercontrol/tray.py acercontrol_tray.py acercontrol/gui_about.py`
- `python3 tools/smoke_phase7.py --quick`
- `python3 tools/smoke_phase7.py`
- `python3 tools/smoke_phase1.py && python3 tools/smoke_phase2.py && python3 tools/smoke_phase3.py && python3 tools/smoke_phase4.py && python3 tools/smoke_phase5.py && python3 tools/smoke_phase6.py && python3 tools/smoke_phase7.py`

Manual:
- Complete `.planning/phases/07-tray-helper-hardware-compatibility/07-HUMAN-UAT.md` on Ubuntu/PHN16-72.
</verification>

<success_criteria>
Plan 07-03 is complete when hardware compatibility fixtures pass, the packaging Recommends handoff is source-gated, human UAT is documented, full Phase 1-7 smoke gates pass, and Phase 7 is ready for Ubuntu tray/hardware validation.
</success_criteria>
