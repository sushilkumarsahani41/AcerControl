---
phase: 06-boot-persistence-suspend-resume
plan: 01
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - tools/smoke_phase6.py
  - data/acer-performance.service
  - data/acer-performance@.service
  - acercontrol/systemd.py
  - libexec/acercontrol-manage-service
autonomous: true
requirements: [BOOT-01, BOOT-02]
user_setup: []
must_haves:
  truths:
    - "Boot profile persistence is implemented through systemd units and existing real-binary wrappers, never through shell snippets that echo into sysfs."
    - "The stable public unit is acer-performance.service; the templated immediate-apply unit is acer-performance@.service."
    - "acercontrol-manage-service remains allowlist-only and rejects arbitrary services."
    - "All systemd/status helper code is stdlib-only and safe on macOS/non-systemd hosts."
  artifacts:
    - path: "tools/smoke_phase6.py"
      provides: "Source/static gates for Phase 6 unit files, wrappers, systemd facade, GUI wiring, and resume controller"
    - path: "data/acer-performance.service"
      provides: "Stable boot service that reads /etc/default/acercontrol and applies BOOT_PROFILE"
    - path: "data/acer-performance@.service"
      provides: "Templated immediate apply unit whose instance is a validated kernel profile value"
    - path: "acercontrol/systemd.py"
      provides: "GTK-free facade for boot profile config parsing, service status, and bounded startup wait"
    - path: "libexec/acercontrol-manage-service"
      provides: "Strict allowlist for stable service plus known profile template instances"
---

<objective>
Create the Phase 6 boot persistence substrate: smoke runner, stable and templated systemd units, a stdlib systemd/config facade, and an extended service-management wrapper allowlist.
</objective>

<execution_context>
@/Users/sushilkumarsahani/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sushilkumarsahani/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-boot-persistence-suspend-resume/06-RESEARCH.md
@.planning/phases/06-boot-persistence-suspend-resume/06-VALIDATION.md
@.planning/phases/06-boot-persistence-suspend-resume/06-PATTERNS.md
@.planning/phases/05-live-sensors-notifications/05-01-SUMMARY.md
@acercontrol/profiles.py
@acercontrol/privilege.py
@libexec/acercontrol-setprofile
@libexec/acercontrol-set-boot-profile
@libexec/acercontrol-manage-service
@tools/smoke_phase5.py

<interfaces>
- `libexec/acercontrol-setprofile <kernel-value>` validates kernel values and writes platform_profile when run as root.
- `libexec/acercontrol-set-boot-profile <kernel-value>` atomically writes `/etc/default/acercontrol` as `BOOT_PROFILE=<kernel-value>`.
- `acercontrol.privilege.run_privileged()` handles wrapper resolution and pkexec cancellation.
- Existing Phase 1-5 smoke suites must continue passing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase 6 smoke runner wave 0</name>
  <files>tools/smoke_phase6.py</files>
  <read_first>
    @tools/smoke_phase5.py
    @tools/smoke_phase3.py
    @.planning/phases/06-boot-persistence-suspend-resume/06-UI-SPEC.md
    @.planning/phases/06-boot-persistence-suspend-resume/06-RESEARCH.md
    @.planning/phases/06-boot-persistence-suspend-resume/06-VALIDATION.md
    @.planning/phases/06-boot-persistence-suspend-resume/06-PATTERNS.md
  </read_first>
  <action>Create `tools/smoke_phase6.py` using the same project-root bootstrap, `run()` helper, `--quick` mode, `_non_comment_text()` helper, inline scenario functions, and pass-count pattern as `tools/smoke_phase5.py`. Quick mode must pass before Phase 6 implementation files exist by SKIPing implementation-file-specific scenarios. The runner itself must not import `gi`, must not invoke real mutating `systemctl`, `pkexec`, `sudo`, or write sysfs. Add initial scenarios for: Phase 6 planning docs exist; stable unit contract if `data/acer-performance.service` exists; templated unit contract if `data/acer-performance@.service` exists; `acercontrol/systemd.py` is stdlib-only and contains bounded subprocess timeouts if it exists; `libexec/acercontrol-manage-service` allows stable service plus allowed template instances and still rejects unrelated services; future placeholders for GUI boot panel and resume controller that SKIP until files exist. Full mode must py_compile `tools/smoke_phase6.py` and any existing new Python files.</action>
  <verify>
    <automated>python3 tools/smoke_phase6.py --quick</automated>
    <automated>python3 -m py_compile tools/smoke_phase6.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tools/smoke_phase6.py --quick` exits 0 before Phase 6 implementation files exist.
    - The smoke runner contains strict source checks for both unit files, wrapper allowlist shape, systemd facade timeouts, GUI boot panel, MainWindow boot panel ordering, profile write wait guard, and login1 resume subscription.
    - The runner contains no `import gi`, no `pkexec`, no `sudo`, and no mutating `systemctl` command invocation.
  </acceptance_criteria>
  <done>Phase 6 has a side-effect-free source/static gate before implementation begins.</done>
</task>

<task type="auto">
  <name>Task 2: Add systemd boot units</name>
  <files>data/acer-performance.service, data/acer-performance@.service</files>
  <read_first>
    @.planning/phases/06-boot-persistence-suspend-resume/06-RESEARCH.md
    @libexec/acercontrol-setprofile
    @libexec/acercontrol-set-boot-profile
  </read_first>
  <action>Create `data/acer-performance.service` as the stable public boot unit. It must include `Description=Apply AcerControl configured performance profile`, `ConditionKernelModuleLoaded=acer_wmi`, `ConditionPathExists=/sys/firmware/acpi/platform_profile`, `After=systemd-modules-load.service`, `Conflicts=power-profiles-daemon.service`, `Before=graphical.target`, `Type=oneshot`, `RemainAfterExit=yes`, `Environment=BOOT_PROFILE=balanced`, `EnvironmentFile=-/etc/default/acercontrol`, `ExecStart=/usr/libexec/acercontrol/acercontrol-setprofile ${BOOT_PROFILE}`, and `[Install] WantedBy=graphical.target`. Create `data/acer-performance@.service` as the templated immediate-apply unit with the same critical unit/service directives but `Description=Apply AcerControl performance profile %i` and `ExecStart=/usr/libexec/acercontrol/acercontrol-setprofile %i`. Do not use `/bin/sh`, `bash`, `tee`, or direct sysfs writes in either unit.</action>
  <verify>
    <automated>python3 tools/smoke_phase6.py --quick</automated>
  </verify>
  <acceptance_criteria>
    - Stable unit contains every BOOT-01 directive listed in the action.
    - Template unit contains every BOOT-01 directive listed in the action and uses `%i`.
    - Neither unit contains `bash`, `/bin/sh`, `tee`, `echo`, or `/sys/firmware/acpi/platform_profile` in `ExecStart`.
  </acceptance_criteria>
  <done>Boot persistence unit files exist and delegate writes to the real profile wrapper.</done>
</task>

<task type="auto">
  <name>Task 3: Add stdlib systemd facade</name>
  <files>acercontrol/systemd.py</files>
  <read_first>
    @acercontrol/features.py
    @acercontrol/profiles.py
    @acercontrol/core.py
    @.planning/phases/06-boot-persistence-suspend-resume/06-PATTERNS.md
  </read_first>
  <action>Create `acercontrol/systemd.py` as a stdlib-only module. Define constants `BOOT_CONFIG_PATH = Path("/etc/default/acercontrol")`, `BOOT_SERVICE = "acer-performance.service"`, `BOOT_TEMPLATE_PREFIX = "acer-performance@"`, and `BOOT_WAIT_TIMEOUT = 5`. Implement defensive helpers: `read_boot_profile() -> Profile` parses `BOOT_PROFILE=<kernel-value>` and returns `Profile.BALANCED` when config is missing/invalid; `boot_instance_for_profile(profile_name: str) -> str` returns `acer-performance@<kernel-value>.service` after validating user-facing profile names against `PROFILES`; `service_enabled(service=BOOT_SERVICE) -> str` returns `enabled`, `disabled`, `not-found`, or `unknown`; `service_active(service=BOOT_SERVICE) -> str` returns systemctl output or `unknown`; `wait_for_boot_service(timeout=BOOT_WAIT_TIMEOUT) -> bool` calls `subprocess.run(["systemctl", "is-active", "--wait", BOOT_SERVICE], capture_output=True, text=True, timeout=timeout)` and returns True only for rc 0. No helper may raise for missing `systemctl`, timeout, missing config, or invalid file content.</action>
  <verify>
    <automated>python3 -m py_compile acercontrol/systemd.py</automated>
    <automated>python3 tools/smoke_phase6.py --quick</automated>
  </verify>
  <acceptance_criteria>
    - `acercontrol/systemd.py` imports only stdlib and `acercontrol.profiles`.
    - `wait_for_boot_service` contains `["systemctl", "is-active", "--wait", BOOT_SERVICE]` and a timeout.
    - `read_boot_profile` maps kernel values back through `Profile`, never raw string comparisons in GUI.
    - Missing config and missing systemctl return safe defaults, not exceptions.
  </acceptance_criteria>
  <done>Service/config behavior is centralized outside GTK code.</done>
</task>

<task type="auto">
  <name>Task 4: Extend service wrapper allowlist</name>
  <files>libexec/acercontrol-manage-service</files>
  <read_first>
    @libexec/acercontrol-manage-service
    @libexec/acercontrol-disable-ppd
    @acercontrol/profiles.py
    @tools/smoke_phase2.py
    @tools/smoke_phase6.py
  </read_first>
  <action>Modify `libexec/acercontrol-manage-service` so `ALLOWED_SERVICES` includes exactly `acer-performance.service` plus the five templated service instances for the allowed kernel values: `acer-performance@low-power.service`, `acer-performance@quiet.service`, `acer-performance@balanced.service`, `acer-performance@balanced-performance.service`, and `acer-performance@performance.service`. Preserve the strict action allowlist and non-root behavior. Do not add wildcard matching, glob matching, shell=True, or arbitrary service names. Update comments to remove the Phase 6 open-question note.</action>
  <verify>
    <automated>python3 tools/smoke_phase6.py --quick</automated>
    <automated>python3 tools/smoke_phase2.py</automated>
  </verify>
  <acceptance_criteria>
    - Wrapper rejects unrelated services before calling systemctl.
    - Wrapper accepts only the stable unit and known kernel-value template instances.
    - Existing Phase 2 smoke remains green.
  </acceptance_criteria>
  <done>Privileged service management can operate on Phase 6 units without loosening the boundary.</done>
</task>

</tasks>

<verification>
Automated:
- `python3 -m py_compile acercontrol/systemd.py tools/smoke_phase6.py`
- `python3 tools/smoke_phase6.py --quick`
- `python3 tools/smoke_phase6.py`
- `python3 tools/smoke_phase1.py && python3 tools/smoke_phase2.py && python3 tools/smoke_phase3.py && python3 tools/smoke_phase4.py && python3 tools/smoke_phase5.py && python3 tools/smoke_phase6.py`

Manual PHN16-72 UAT after all Phase 6 plans:
- Copy/install unit files, run `systemctl daemon-reload`, enable boot service, set a boot profile, cold boot, and verify `acercontrol get` before GUI launch.
</verification>

<success_criteria>
Plan 06-01 is complete when boot unit files, the systemd facade, the strict service wrapper allowlist, and the Phase 6 smoke runner exist; all source/static gates pass; and previous phase smoke suites remain green.
</success_criteria>
