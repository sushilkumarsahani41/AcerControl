---
phase: 06-boot-persistence-suspend-resume
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - tools/smoke_phase6.py
  - acercontrol/gui_resume.py
  - acercontrol/gui_window.py
autonomous: true
requirements: [BOOT-05]
user_setup: []
must_haves:
  truths:
    - "Resume handling subscribes to org.freedesktop.login1 PrepareForSleep using Gio, with no new dependencies."
    - "Before-sleep signals are ignored; after-resume signals trigger a best-effort read-back and reapply check."
    - "Resume reapply uses the existing privileged profile wrapper and does not introduce a root daemon."
    - "Missing system bus or login1 support never crashes the GUI."
  artifacts:
    - path: "acercontrol/gui_resume.py"
      provides: "ResumeReapplyController for login1 subscription and cleanup"
    - path: "acercontrol/gui_window.py"
      provides: "last-selected profile tracking, resume reapply hook, and close cleanup"
---

<objective>
Add suspend/resume profile restoration by subscribing to login1 `PrepareForSleep` and reapplying the last selected non-custom profile after resume when firmware or other services changed it.
</objective>

<execution_context>
@/Users/sushilkumarsahani/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sushilkumarsahani/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-boot-persistence-suspend-resume/06-UI-SPEC.md
@.planning/phases/06-boot-persistence-suspend-resume/06-RESEARCH.md
@.planning/phases/06-boot-persistence-suspend-resume/06-PATTERNS.md
@.planning/phases/06-boot-persistence-suspend-resume/06-02-PLAN.md
@acercontrol/gui_window.py
@acercontrol/gui_notifications.py
@acercontrol/gui_profiles.py
@acercontrol/privilege.py
@acercontrol/profiles.py
@acercontrol/core.py
@tools/smoke_phase6.py

<interfaces>
- `MainWindow.notify_profile_change(profile_name)` already updates `_last_seen_profile_name`.
- `read_profile()` returns `Profile.CUSTOM` on missing/unmapped state.
- `run_privileged(["acercontrol-setprofile", kernel_value])` is the only direct profile write path.
- `Gio` is already available in GUI modules; no `dasbus` or `pydbus` dependency is allowed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Phase 6 smoke for login1 resume</name>
  <files>tools/smoke_phase6.py</files>
  <read_first>
    @tools/smoke_phase6.py
    @.planning/phases/06-boot-persistence-suspend-resume/06-RESEARCH.md
    @.planning/phases/06-boot-persistence-suspend-resume/06-PATTERNS.md
  </read_first>
  <action>Extend `tools/smoke_phase6.py` so quick mode SKIPs `gui_resume.py` checks until the file exists, then strictly checks that `gui_resume.py` contains `ResumeReapplyController`, `Gio.BusType.SYSTEM`, `signal_subscribe`, `org.freedesktop.login1`, `PrepareForSleep`, `/org/freedesktop/login1`, `signal_unsubscribe`, and a branch that ignores before-sleep and handles after-resume. Add MainWindow checks for `_resume_controller`, `reapply_last_profile_after_resume`, `Profile restored after resume`, and cleanup in close-request. Add forbidden-token checks for `dasbus`, `pydbus`, `threading.Thread`, `time.sleep(`, direct `pkexec`, and direct `sudo` in `gui_resume.py`.</action>
  <verify>
    <automated>python3 tools/smoke_phase6.py --quick</automated>
  </verify>
  <acceptance_criteria>
    - Smoke runner enforces login1 subscription shape and cleanup.
    - Smoke runner enforces no new D-Bus/runtime dependencies.
    - Smoke runner enforces MainWindow resume hook and user-facing toast copy.
  </acceptance_criteria>
  <done>Resume requirements are source-gated before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Add ResumeReapplyController</name>
  <files>acercontrol/gui_resume.py</files>
  <read_first>
    @acercontrol/gui_notifications.py
    @acercontrol/gui_window.py
    @.planning/phases/06-boot-persistence-suspend-resume/06-RESEARCH.md
  </read_first>
  <action>Create `acercontrol/gui_resume.py` as a GTK/Gio GUI-only module defining `ResumeReapplyController(window)`. In `start()`, obtain the system bus with `Gio.bus_get_sync(Gio.BusType.SYSTEM, None)` and subscribe to `org.freedesktop.login1.Manager.PrepareForSleep` on `/org/freedesktop/login1`. The callback must parse the boolean `start` argument; if true, return immediately; if false, call `window.reapply_last_profile_after_resume()`. Store the subscription ID and connection. Implement `stop()` that calls `signal_unsubscribe(subscription_id)` when available and clears state. Catch `GLib.Error`, `AttributeError`, and `TypeError` around subscription setup so non-Linux/macOS hosts degrade without raising.</action>
  <verify>
    <automated>python3 -m py_compile acercontrol/gui_resume.py</automated>
    <automated>python3 tools/smoke_phase6.py --quick</automated>
  </verify>
  <acceptance_criteria>
    - Controller subscribes to system login1 PrepareForSleep.
    - Before-sleep signal does nothing.
    - After-resume signal delegates to MainWindow.
    - `stop()` unsubscribes.
    - Module contains no `dasbus`, no `pydbus`, no thread, and no sleep loop.
  </acceptance_criteria>
  <done>login1 resume detection is isolated and cleanup-capable.</done>
</task>

<task type="auto">
  <name>Task 3: Add MainWindow resume reapply hook</name>
  <files>acercontrol/gui_window.py</files>
  <read_first>
    @acercontrol/gui_window.py
    @acercontrol/gui_resume.py
    @acercontrol/core.py
    @acercontrol/profiles.py
    @acercontrol/privilege.py
  </read_first>
  <action>Modify `acercontrol/gui_window.py` to import `PROFILES`, `read_profile`, and `run_privileged` if not already present in the needed form, and import `ResumeReapplyController`. Add `self._resume_controller = ResumeReapplyController(self)` in `__init__` and call `self._resume_controller.start()` after the window content is set. Track a last selected non-custom user-facing profile name, initialized from the current profile when not custom and updated in `notify_profile_change(profile_name)` when the profile is not `custom`. Add `reapply_last_profile_after_resume(self) -> None`: if there is no last selected non-custom profile, return; read the actual profile; if it already matches, return; otherwise call `run_privileged(["acercontrol-setprofile", PROFILES[last_selected]])`; on success, refresh the profile panel, update `_last_seen_profile_name`, and show `Profile restored after resume`; on cancellation/failure, do not spam repeated notifications. Update close-request cleanup to stop the resume controller in addition to stopping the sensor timer.</action>
  <verify>
    <automated>python3 -m py_compile acercontrol/gui_window.py acercontrol/gui_resume.py</automated>
    <automated>python3 tools/smoke_phase6.py --quick</automated>
  </verify>
  <acceptance_criteria>
    - `MainWindow` owns `_resume_controller`.
    - `notify_profile_change` updates last selected profile only for non-custom names.
    - `reapply_last_profile_after_resume` compares actual read-back before writing.
    - Reapply uses `run_privileged(["acercontrol-setprofile", PROFILES[` and exact toast `Profile restored after resume`.
    - Close cleanup stops resume subscription.
  </acceptance_criteria>
  <done>MainWindow can restore the last selected profile after resume without direct sysfs writes.</done>
</task>

<task type="auto">
  <name>Task 4: Final Phase 6 regression pass</name>
  <files>tools/smoke_phase6.py, acercontrol/gui_window.py</files>
  <read_first>
    @tools/smoke_phase6.py
    @tools/smoke_phase1.py
    @tools/smoke_phase2.py
    @tools/smoke_phase3.py
    @tools/smoke_phase4.py
    @tools/smoke_phase5.py
  </read_first>
  <action>Run the full Phase 1-6 smoke chain. If Phase 6 full smoke finds source-token formatting failures, fix formatting without changing behavior. If earlier phase smoke fails due to a real regression, fix the regression in the owning file. Do not weaken previous phase gates.</action>
  <verify>
    <automated>python3 -m py_compile acercontrol/systemd.py acercontrol/gui_boot.py acercontrol/gui_resume.py acercontrol/gui_window.py acercontrol/gui_profiles.py tools/smoke_phase6.py</automated>
    <automated>python3 tools/smoke_phase6.py</automated>
    <automated>python3 tools/smoke_phase1.py && python3 tools/smoke_phase2.py && python3 tools/smoke_phase3.py && python3 tools/smoke_phase4.py && python3 tools/smoke_phase5.py && python3 tools/smoke_phase6.py</automated>
  </verify>
  <acceptance_criteria>
    - Full Phase 6 smoke exits 0.
    - Full Phase 1-5 smoke still exits 0.
    - No direct sysfs, direct pkexec/sudo, or arbitrary service control was introduced.
  </acceptance_criteria>
  <done>Phase 6 implementation is source/static verified and ready for PHN16-72 cold-boot/resume UAT.</done>
</task>

</tasks>

<verification>
Automated:
- `python3 -m py_compile acercontrol/systemd.py acercontrol/gui_boot.py acercontrol/gui_resume.py acercontrol/gui_window.py acercontrol/gui_profiles.py tools/smoke_phase6.py`
- `python3 tools/smoke_phase6.py --quick`
- `python3 tools/smoke_phase6.py`
- `python3 tools/smoke_phase1.py && python3 tools/smoke_phase2.py && python3 tools/smoke_phase3.py && python3 tools/smoke_phase4.py && python3 tools/smoke_phase5.py && python3 tools/smoke_phase6.py`

Manual PHN16-72 UAT:
- Suspend/resume with the GUI running. If firmware or PPD changes profile, the GUI restores the last selected non-custom profile after `PrepareForSleep(false)`.
- Confirm no repeated restore toasts or notification spam after repeated resume cycles.
</verification>

<success_criteria>
Plan 06-03 is complete when login1 resume detection is wired, after-resume reapply is best-effort and read-back-driven, cleanup is reliable, all Phase 1-6 smoke gates pass, and hardware UAT steps are documented in the summary.
</success_criteria>
