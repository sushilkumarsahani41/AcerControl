---
phase: 06-boot-persistence-suspend-resume
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - tools/smoke_phase6.py
  - acercontrol/gui_boot.py
  - acercontrol/gui_window.py
  - acercontrol/gui_profiles.py
autonomous: true
requirements: [BOOT-03, BOOT-04]
user_setup: []
must_haves:
  truths:
    - "Boot service controls appear after profile controls and live sensors in the shared MainWindow page."
    - "The GUI never invokes pkexec, sudo, or mutating systemctl directly; privileged service/config changes go through existing wrappers."
    - "The GUI performs a bounded boot-service wait before the first profile write path."
    - "The boot panel shows user-facing profile names only."
  artifacts:
    - path: "acercontrol/gui_boot.py"
      provides: "BootServicePanel for status, enable/disable, boot profile selection, and apply-now behavior"
    - path: "acercontrol/gui_window.py"
      provides: "Boot panel ordering and MainWindow.ensure_boot_service_ready()"
    - path: "acercontrol/gui_profiles.py"
      provides: "Profile-click guard that waits for the boot service before privileged profile writes"
---

<objective>
Add the Phase 6 GUI boot service panel and startup race guard so users can enable boot persistence, choose a boot profile, apply it immediately, and avoid first-write races with the boot unit.
</objective>

<execution_context>
@/Users/sushilkumarsahani/.codex/get-shit-done/workflows/execute-plan.md
@/Users/sushilkumarsahani/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-boot-persistence-suspend-resume/06-UI-SPEC.md
@.planning/phases/06-boot-persistence-suspend-resume/06-RESEARCH.md
@.planning/phases/06-boot-persistence-suspend-resume/06-PATTERNS.md
@.planning/phases/06-boot-persistence-suspend-resume/06-01-PLAN.md
@.planning/phases/05-live-sensors-notifications/05-01-SUMMARY.md
@acercontrol/systemd.py
@acercontrol/gui_window.py
@acercontrol/gui_profiles.py
@acercontrol/gui_sensors.py
@acercontrol/privilege.py
@acercontrol/profiles.py
@tools/smoke_phase6.py

<interfaces>
- `acercontrol.systemd.read_boot_profile()` returns a `Profile`.
- `acercontrol.systemd.service_enabled()` and `service_active()` return strings and do not raise.
- `acercontrol.systemd.boot_instance_for_profile(profile_name)` returns the safe templated service instance for a user-facing name.
- `run_privileged()` is the only GUI mutation path for boot config and service actions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Phase 6 smoke for boot panel and startup wait</name>
  <files>tools/smoke_phase6.py</files>
  <read_first>
    @tools/smoke_phase6.py
    @.planning/phases/06-boot-persistence-suspend-resume/06-UI-SPEC.md
    @.planning/phases/06-boot-persistence-suspend-resume/06-PATTERNS.md
  </read_first>
  <action>Extend `tools/smoke_phase6.py` so quick mode SKIPs `gui_boot.py` checks until the file exists, then strictly checks that `gui_boot.py` contains `BootServicePanel`, `Boot Service`, `Apply a profile during startup and after resume.`, `acer-performance.service`, `Enable at boot`, `Boot profile`, `Apply now`, `Boot profile updated`, `Boot service enabled`, `Boot service disabled`, `Authorization cancelled`, and `Boot service update failed. See terminal for details.`. Add checks that `gui_boot.py` uses `run_privileged`, `acercontrol-set-boot-profile`, `acercontrol-manage-service`, and `boot_instance_for_profile`, and does not contain direct `pkexec`, `sudo`, or mutating `systemctl` subprocess calls. Add `gui_window.py` checks for `BootServicePanel`, `_boot_panel`, `SensorPanel(self)` before `BootServicePanel(self)`, and `ensure_boot_service_ready`. Add `gui_profiles.py` check that `_on_profile_clicked` calls `ensure_boot_service_ready` before `run_privileged(["acercontrol-setprofile"`.</action>
  <verify>
    <automated>python3 tools/smoke_phase6.py --quick</automated>
  </verify>
  <acceptance_criteria>
    - Smoke runner enforces boot panel copy and wrapper-only mutation path.
    - Smoke runner enforces MainWindow panel ordering.
    - Smoke runner enforces first-write wait guard location.
  </acceptance_criteria>
  <done>Boot panel and startup wait expectations are executable before GUI edits.</done>
</task>

<task type="auto">
  <name>Task 2: Add BootServicePanel</name>
  <files>acercontrol/gui_boot.py</files>
  <read_first>
    @acercontrol/gui_sensors.py
    @acercontrol/gui_profiles.py
    @acercontrol/systemd.py
    @acercontrol/privilege.py
    @acercontrol/profiles.py
    @.planning/phases/06-boot-persistence-suspend-resume/06-UI-SPEC.md
  </read_first>
  <action>Create `acercontrol/gui_boot.py` as a GTK4/libadwaita module defining `BootServicePanel(Adw.PreferencesGroup)`. The group title must be `Boot Service` and description `Apply a profile during startup and after resume.`. Add visible controls/status for `acer-performance.service`, `Enable at boot`, `Boot profile`, and `Apply now`. Use profile choices in exact `("eco", "quiet", "balanced", "performance", "turbo")` order. On refresh, call `acercontrol.systemd.service_enabled()`, `service_active()`, and `read_boot_profile()` to render actual state. On enable, call `run_privileged(["acercontrol-manage-service", "enable", "acer-performance.service"])`. On disable, call `run_privileged(["acercontrol-manage-service", "disable", "acer-performance.service"])`. On boot profile change or apply-now, call `run_privileged(["acercontrol-set-boot-profile", PROFILES[profile_name]])`, then `run_privileged(["acercontrol-manage-service", "start", boot_instance_for_profile(profile_name)])`. Use `window.show_toast()` for exact success/cancel/failure copy from the UI spec. Do not shell out directly from this file except through `run_privileged`.</action>
  <verify>
    <automated>python3 -m py_compile acercontrol/gui_boot.py</automated>
    <automated>python3 tools/smoke_phase6.py --quick</automated>
  </verify>
  <acceptance_criteria>
    - `BootServicePanel` exists and renders all required copy.
    - Mutations use `run_privileged` and wrapper names only.
    - UI displays only user-facing profile names.
    - Cancellation refreshes actual state and shows `Authorization cancelled`.
  </acceptance_criteria>
  <done>Boot persistence controls are implemented as a reusable Adwaita group.</done>
</task>

<task type="auto">
  <name>Task 3: Wire boot panel into MainWindow</name>
  <files>acercontrol/gui_window.py</files>
  <read_first>
    @acercontrol/gui_window.py
    @acercontrol/gui_boot.py
    @acercontrol/systemd.py
    @tools/smoke_phase6.py
  </read_first>
  <action>Modify `acercontrol/gui_window.py` to import `wait_for_boot_service` from `acercontrol.systemd` and `BootServicePanel` from `acercontrol.gui_boot`. In `__init__`, add `self._boot_service_waited = False` and `self._boot_panel = BootServicePanel(self)`. Append it to the existing shared `Adw.PreferencesPage` after `self._sensor_panel` and keep `ProfileControlPanel(self)` first, `SensorPanel(self)` second, `BootServicePanel(self)` third. Add `ensure_boot_service_ready(self) -> bool` that runs the bounded wait exactly once by calling `wait_for_boot_service()` and stores the result; it must not raise and must not loop forever. In `_route()`, after blockers pass and banners rebuild, call `self.ensure_boot_service_ready()` before `_ensure_sensor_refresh()`. Keep all Phase 5 sensor timer cleanup behavior intact.</action>
  <verify>
    <automated>python3 -m py_compile acercontrol/gui_window.py acercontrol/gui_boot.py</automated>
    <automated>python3 tools/smoke_phase6.py --quick</automated>
    <automated>python3 tools/smoke_phase5.py</automated>
  </verify>
  <acceptance_criteria>
    - MainWindow source contains `BootServicePanel`, `_boot_panel`, and `ensure_boot_service_ready`.
    - Source order is profile panel, sensor panel, boot panel.
    - `_route()` starts/stops sensors as before and calls the boot wait before live refresh.
    - Phase 5 smoke remains green.
  </acceptance_criteria>
  <done>The main page includes the boot panel and a startup wait guard.</done>
</task>

<task type="auto">
  <name>Task 4: Guard profile writes behind boot wait</name>
  <files>acercontrol/gui_profiles.py</files>
  <read_first>
    @acercontrol/gui_profiles.py
    @acercontrol/gui_window.py
    @tools/smoke_phase6.py
  </read_first>
  <action>Modify `ProfileControlPanel._on_profile_clicked()` so, after validating it is not pending and the requested profile differs from active state, it calls `self._window.ensure_boot_service_ready()` when the method exists, before setting pending UI and before `run_privileged(["acercontrol-setprofile", ...])`. The call must be best-effort: a false return must not block the user from changing profiles, but the wait must have been attempted. Preserve exact Phase 4 cancellation, generic failure, success fallback, and mismatch copy strings.</action>
  <verify>
    <automated>python3 -m py_compile acercontrol/gui_profiles.py acercontrol/gui_window.py acercontrol/gui_boot.py</automated>
    <automated>python3 tools/smoke_phase6.py --quick</automated>
    <automated>python3 tools/smoke_phase4.py && python3 tools/smoke_phase5.py</automated>
  </verify>
  <acceptance_criteria>
    - `ensure_boot_service_ready` appears before the setprofile `run_privileged` call in `_on_profile_clicked`.
    - Existing Phase 4/5 profile behavior and smoke suites remain green.
  </acceptance_criteria>
  <done>First profile write path cannot race the boot unit without at least attempting the bounded wait.</done>
</task>

</tasks>

<verification>
Automated:
- `python3 -m py_compile acercontrol/gui_boot.py acercontrol/gui_window.py acercontrol/gui_profiles.py`
- `python3 tools/smoke_phase6.py --quick`
- `python3 tools/smoke_phase6.py`
- `python3 tools/smoke_phase1.py && python3 tools/smoke_phase2.py && python3 tools/smoke_phase3.py && python3 tools/smoke_phase4.py && python3 tools/smoke_phase5.py && python3 tools/smoke_phase6.py`

Manual PHN16-72 UAT after all Phase 6 plans:
- Toggle boot service from GUI and verify `systemctl is-enabled acer-performance.service` matches.
- Change boot profile from GUI and verify `/etc/default/acercontrol` stores a kernel value while the GUI shows a user-facing name.
- Launch GUI immediately after login and click profile quickly; verify the boot unit does not clobber the chosen profile.
</verification>

<success_criteria>
Plan 06-02 is complete when the boot service panel is visible after sensors, wrapper-backed boot profile/service actions work at the source level, the first-write wait guard is in both route and profile-click paths, and Phase 1-6 smoke gates pass on this host.
</success_criteria>
